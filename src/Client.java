
public class Client {
	/* (2 Threads: one to send one to receive transactions)
	 * - Reads all transactions from file 'transaction.txt' and save them in transaction[] (transaction class objects)
	 * - Able to transfer transactions to network buffer using send() method of Network. Yields CPU in case buffer full.
	 * - client retrieves transactions from network using receive() method of Network. Display on console. Yields CPU if buffer full.
	 */
}
